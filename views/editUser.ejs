<!DOCTYPE html>
<html>
<head>
    <title>Edit Party</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f8;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .card {
            border-radius: 12px;
        }
        .form-label {
            font-weight: 600;
        }
        h3 {
            font-weight: bold;
            color: #333;
        }
        .container {
            max-width: 800px;
        }
    </style>
</head>
<body>

<div class="container mt-5">
    <div class="card p-4 shadow">
        <h3 class="text-center mb-4">Edit Party</h3>

        <form action="/users/edit/<%= user._id %>" method="POST">
            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label">Party Name</label>
                    <input type="text" class="form-control" name="partyName" value="<%= user.partyName %>" required>
                </div>
                <div class="col-md-6">
                    <label class="form-label">GSTIN</label>
                    <input type="text" class="form-control" name="gstin" value="<%= user.gstin %>" required>
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label">Address</label>
                <input type="text" class="form-control" name="address" value="<%= user.address %>" required>
            </div>

            <div class="row mb-3">
                <div class="col-md-4">
                    <label class="form-label">Mobile</label>
                    <input type="text" class="form-control" name="mobile" value="<%= user.mobile %>" required>
                </div>
                <div class="col-md-4">
                    <label class="form-label">PAN No</label>
                    <input type="text" class="form-control" name="panno" value="<%= user.panno %>" required>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Email</label>
                    <input type="email" class="form-control" name="email" value="<%= user.email %>">
                </div>
            </div>

            <div class="text-center">
                <button type="submit" class="btn btn-primary px-4">Update Party</button>
                <a href="/users" class="btn btn-secondary px-4">Cancel</a>
            </div>
        </form>
    </div>
</div>

</body>
</html>
