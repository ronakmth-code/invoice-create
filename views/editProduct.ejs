<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Product</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .form-inline-fields {
            display: flex;
            gap: 10px;
            align-items: flex-end;
        }
        .form-inline-fields .form-group {
            display: flex;
            flex-direction: column;
        }
        .form-inline-fields input {
            min-width: 100px;
        }
    </style>
</head>
<body class="container mt-4">
    <h2 class="mb-4">Edit Product</h2>

    <form action="/products/edit/<%= product._id %>" method="POST">
        <div class="form-inline-fields">
            <div class="form-group">
                <label>HSN</label>
                <input type="text" name="hsn" value="<%= product.hsn %>" class="form-control" required>
            </div>
            <div class="form-group">
                <label>Product Name</label>
                <input type="text" name="pname" value="<%= product.pname %>" class="form-control" required>
            </div>
            <div class="form-group">
                <label>Pack</label>
                <input type="text" name="pack" value="<%= product.pack %>" class="form-control">
            </div>
            <div class="form-group">
                <label>Batch</label>
                <input type="text" name="batch" value="<%= product.batch %>" class="form-control">
            </div>
            <div class="form-group">
                <label>Exp</label>
                <input type="text" name="exp" value="<%= product.exp %>" class="form-control">
            </div>
            <div class="form-group">
                <label>M.R.P</label>
                <input type="text" name="mrp" value="<%= product.mrp %>" class="form-control">
            </div>
            <div class="form-group">
                <label>Rate</label>
                <input type="text" name="rate" value="<%= product.rate %>" class="form-control">
            </div>
            <div class="form-group">
                <label>IGST%</label>
                <input type="text" name="igst" value="<%= product.igst %>" class="form-control">
            </div>
        </div>

        <div class="mt-3">
            <button type="submit" class="btn btn-success">Update Product</button>
            <a href="/product" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</body>
</html>
